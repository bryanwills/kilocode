---
"kilo-code": minor
"@kilocode/cli": minor
---

Rate assistant responses with thumbs up/down. Click the thumbs buttons next to the copy button on any assistant message, or press `<leader>+` / `<leader>-` in the terminal UI. Feedback is sent to Kilo to help improve model and prompt quality and only appears when telemetry is enabled.
